# Vendored from ninjapaw/pawprint and compared byte-for-byte against it.
# Formatting these locally is exactly the drift the kit gate rejects.
vendor/

# Generated by `az bicep build`. Reformatting breaks the committed-ARM drift
# check, which compares against freshly compiled output.
infra/azure/**/azuredeploy.json

node_modules/
web/node_modules/
api/node_modules/
web/dist/
api/dist/
package-lock.json
